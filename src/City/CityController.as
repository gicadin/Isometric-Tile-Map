package City 
{
	import flash.events.EventDispatcher;
	
	/**
	 * This class receives events from the buttons and other inputs within the CitiView 
	 * and updates the model accordingly. It is the model's duty to propagate those updates
	 * to the view.
	 * 
	 * The controller is also in charge of connecting to the server to retrieve and store data
	 */
	public class CityController
	{
		
		public function CityController() 
		{
			
		}
		
	}

}